import 'package:flutter/material.dart';

class FoodExclusiveScreen extends StatelessWidget {
  const FoodExclusiveScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Food exclusion",),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
